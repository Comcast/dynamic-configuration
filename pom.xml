<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.comcast.dynocon</groupId>
    <artifactId>dynocon-parent</artifactId>
    <version>2.0.0</version>
    <packaging>pom</packaging>
    <name>Dynamic Configuration Parent</name>
    <description>JSON Dynamic Properties</description>

    <url>https://github.com/Comcast/dynamic-configuration</url>

    <licenses>
        <license>
            <name>Apache-2.0 License</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/Comcast/dynamic-configuration</url>
        <connection>scm:git:git://github.com/Comcast/dynamic-configuration.git</connection>
        <developerConnection>scm:git:git@github.com:Comcast/dynamic-configuration.git</developerConnection>
    </scm>

    <developers>
        <developer>
            <email>eugene_strokin@comcast.com</email>
            <name>Eugene Strokin</name>
            <id>strokine</id>
        </developer>
        <developer>
            <email>hemanto_bora@comcast.com</email>
            <name>Hemanto Bora</name>
            <id>hbora921</id>
        </developer>
        <developer>
            <email>jhulfikar_kabeer@comcast.com</email>
            <name>Jhulfikar Kabeer</name>
            <id>jkabee200</id>
        </developer>
    </developers>

    <modules>
        <module>dynocon-core</module>
        <module>dynocon-dynamodb</module>
        <module>dynocon-s3</module>
    </modules>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>  <!-- Updated version -->
                <configuration>
                    <forkCount>1</forkCount>
                    <reuseForks>false</reuseForks>
                </configuration>
            </plugin>

            <!-- REMOVED nexus-staging-maven-plugin -->
            <!-- ADD central-publishing-maven-plugin instead -->
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>0.9.0</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>  <!-- Matches server-id in GitHub Actions -->
                    <autoPublish>true</autoPublish>  <!-- Auto-publish without manual staging -->
                    <waitUntil>published</waitUntil>  <!-- Wait for publishing to complete -->
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>  <!-- Updated version -->
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>  <!-- Updated version -->
                <configuration>
                    <release>25</release>  <!-- Added for Java 25 -->
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>3.1.0</version>  <!-- Updated version -->
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- UPDATED Distribution Management for Central Portal -->
    <distributionManagement>
        <snapshotRepository>
            <id>central</id>  <!-- Changed from ossrh -->
        </snapshotRepository>
        <repository>
            <id>central</id>  <!-- Changed from ossrh -->
            <name>Central Repository</name>
        </repository>
    </distributionManagement>
</project>